{
  "rules": {
    "users": {
      ".read": true,
      ".indexOn": ["username"],
      "$uid": {
        ".write": "$uid === auth.uid"
      }
    },
    "attendance": {
      ".read": true,
      "$session_id": {
        ".write": "auth != null && !newData.exists()",
        ".indexOn": ["rollNo", "timestamp"],
        "$record_id": {
          ".write": "auth != null || !data.exists()",
          ".validate": "!newData.exists() || (newData.hasChildren(['name', 'rollNo', 'time', 'timestamp']) && newData.child('name').isString() && newData.child('name').val().length > 0 && newData.child('name').val().length <= 100 && newData.child('rollNo').isString() && newData.child('rollNo').val().length > 0 && newData.child('rollNo').val().length <= 40 && newData.child('time').isString() && newData.child('time').val().length > 0 && newData.child('time').val().length <= 20 && newData.child('timestamp').isNumber())"
        }
      }
    },
    "requests": {
      ".read": true,
      "$session_id": {
        ".write": "auth != null && !newData.exists()",
        "$request_id": {
          ".write": "auth != null || !data.exists()",
          ".validate": "!newData.exists() || (newData.hasChildren(['name', 'rollNo', 'subject', 'requestTime', 'timestamp', 'status']) && newData.child('name').isString() && newData.child('name').val().length > 0 && newData.child('name').val().length <= 100 && newData.child('rollNo').isString() && newData.child('rollNo').val().length > 0 && newData.child('rollNo').val().length <= 40 && newData.child('subject').isString() && newData.child('subject').val().length > 0 && newData.child('subject').val().length <= 80 && newData.child('requestTime').isString() && newData.child('requestTime').val().length > 0 && newData.child('requestTime').val().length <= 20 && newData.child('timestamp').isNumber() && newData.child('status').isString())"
        }
      }
    },
    "qr_sessions": {
      ".read": true,
      "$session_id": {
        ".write": "auth != null",
        ".validate": "!newData.exists() || (newData.hasChildren(['generatedAt', 'expiryTime', 'subject', 'username']) && newData.child('generatedAt').isNumber() && newData.child('expiryTime').isNumber() && newData.child('subject').isString() && newData.child('subject').val().length > 0 && newData.child('subject').val().length <= 80 && newData.child('username').isString() && newData.child('username').val().length > 0 && newData.child('username').val().length <= 120)"
      }
    },
    "class_student_links": {
      ".read": true,
      "$class_id": {
        "$roll_no": {
          ".write": "auth != null",
          ".validate": "newData.isString()"
        }
      }
    },
    "google_user_links": {
      ".read": true,
      "$google_uid": {
        ".write": "auth != null && auth.uid === $google_uid",
        "$class_id": {
          ".validate": "newData.isString()"
        }
      }
    },
    "feedbacks": {
      ".read": "auth != null",
      "$feedback_id": {
        ".write": "true",
        ".validate": "newData.hasChildren(['rating', 'category', 'text', 'timestamp']) && newData.child('rating').isNumber() && newData.child('category').isString() && newData.child('text').isString() && newData.child('timestamp').isNumber()"
      }
    },
    "student_auth_sessions": {
      "$session_key": {
        ".read": true,
        ".write": true,
        ".validate": "!newData.exists() || (newData.hasChildren(['uid', 'ts']) && newData.child('uid').isString() && newData.child('ts').isNumber())"
      }
    }
  }
}
